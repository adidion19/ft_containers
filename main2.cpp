#include "binary_tree.hpp"

int main()
    {
        // let us try to insert some data into tree and try to visualize the tree as well as traverse.
        Binary_tree<char, int> t = new Binary_tree(ft::pair("a", 4));
    }